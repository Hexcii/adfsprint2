<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/beans"
   xmlns:security="http://www.springframework.org/schema/security"
   xmlns:beans="http://www.springframework.org/schema/beans" 
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-3.2.xsd">
       

    <security:http auto-config="true">

        <security:intercept-url pattern="/lecturer/addNew/**" access="ROLE_LECTURER" />
        <security:intercept-url pattern="/lecturer/modify/**" access="ROLE_LECTURER" />
        <security:intercept-url pattern="/student/**" access="ROLE_STUDENT" />

        <security:intercept-url pattern="/admin/**" access="ROLE_ADMIN" />

    </security:http>

  <!-- 

    <http auto-config="true"  use-expressions="true">

        <intercept-url pattern="/songwriter/**" access="hasRole('ROLE_USER')"  />-->

        <!--<intercept-url pattern="/login" access="permitAll" />-->

      <!-- <form-login login-page="/login" default-target-url="/home"

            authentication-failure-url="/login?error"/>

        <logout logout-success-url="/login?logout" />

    </http>  

     --> 

    <security:authentication-manager>

        <security:authentication-provider>

            <security:user-service>

                <security:user name="donna" password="123456" authorities="ROLE_LECTURER" />
                <security:user name="fabien" password="fabien" authorities="ROLE_STUDENT" />

                <security:user name="admin" password="123456" authorities="ROLE_ADMIN" />

            </security:user-service>

        </security:authentication-provider>

    </security:authentication-manager>
    <!--
    <authentication-manager>
		<authentication-provider>
			<password-encoder hash="sha"/>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="
				select username,password, enabled
				from users where username=?"
				
				authorities-by-username-query="
				select u.username, ur.authority from users u, user_roles ur
				where u.user_id = ur.user_id and u.username =? " />
		</authentication-provider>
	</authentication-manager>
	-->

    

</beans:beans>
